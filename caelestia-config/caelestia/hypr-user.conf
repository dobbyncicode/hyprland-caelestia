source = ~/my-vault/caelestia-config/caelestia/Modules/All-About-UX/variables.conf

# ──────────────────────────────
# UX section
# ──────────────────────────────
source = $UX/no-u.conf
source = $UX/monitors.conf
source = $UX/keybinds.conf
source = $UX/inputs.conf

# ──────────────────────────────
# UI section
# ──────────────────────────────
source = $UI/decorations.conf 
source = $UI/animations.conf 
source = $UI/miscs.conf 
source = $UI/borders.conf
